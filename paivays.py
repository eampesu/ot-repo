class paivays:
    def __init__(self, pvm: date, success: str, failure: str):
        self.pvm=pvm
        self.success=success
        self.failure=failure
    
    def updateSuccess(update: str):
        self.success=update
        id=dateSql(self.username)
        dateSql.updateSuccess(update, id)
        
    def updateFailure(update: str):
        self.failure=failure
        id=dateSql(self.username)
        dateSql.updateFailure(update, id)